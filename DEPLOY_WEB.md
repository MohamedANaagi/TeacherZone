# دليل نشر الويب على Firebase Hosting

## الطريقة السريعة (مُوصى بها) 🚀

استخدم الـ script الجاهز:

```bash
./deploy_web.sh
```

هذا الأمر سيقوم بـ:
1. بناء المشروع للويب (Release)
2. نشر الملفات على Firebase Hosting

## الطريقة اليدوية

إذا أردت تنفيذ الأوامر يدوياً:

```bash
# 1. بناء المشروع
flutter build web --release

# 2. نشر على Firebase
firebase deploy --only hosting
```

## ملاحظات مهمة

- تأكد من أنك في المجلد الرئيسي للمشروع
- تأكد من تسجيل الدخول إلى Firebase: `firebase login`
- تأكد من أن المشروع مربوط بـ Firebase: `firebase use teacherzone-eb4fb`

## الموقع بعد النشر

بعد النشر الناجح، سيكون الموقع متاحاً على:
- **Production**: https://teacherzone-eb4fb-35b30.web.app
- **Custom Domain**: (إذا كان لديك domain مخصص)

## استكشاف الأخطاء

إذا واجهت مشاكل:

1. **خطأ في البناء**: 
   ```bash
   flutter clean
   flutter pub get
   flutter build web --release
   ```

2. **خطأ في النشر**:
   ```bash
   firebase login
   firebase use teacherzone-eb4fb
   firebase deploy --only hosting
   ```

3. **مشاكل في CORS أو رفع الملفات**:
   - تحقق من Console في المتصفح (F12)
   - تحقق من Network tab
   - راجع إعدادات Bunny Storage

