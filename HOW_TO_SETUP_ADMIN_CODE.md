# كيفية إعداد كود الأدمن بشكل صحيح

## ✅ الطريقة الجديدة (المبسطة)

**الأدمن يسجل دخول مباشرة من `adminCodes` collection - لا حاجة لـ Document في `codes`**

### Document في Collection `adminCodes` فقط:

```
Collection: adminCodes
Document ID: (auto-generated)

Fields:
- adminCode: "555"              ⭐ الكود الذي ستستخدمه لتسجيل الدخول
- name: "أدمن"                   ⭐ اسم الأدمن
- description: "وصف اختياري"    ⭐ الوصف (اختياري)
- createdAt: (timestamp)         ⭐ تاريخ الإنشاء
```

**هذا كل ما تحتاجه!** ✅

---

## 📝 كيفية الاستخدام

### 1. تسجيل دخول الأدمن:

1. **افتح التطبيق**
2. **اضغط على زر "كود الأدمن"** في شاشة تسجيل الدخول
3. **أدخل الكود:** `555` (من collection `adminCodes` - Field `adminCode`)
4. **سيتم التحقق تلقائياً:**
   - ✅ البحث عن `adminCode: "555"` في `adminCodes` → موجود
   - ✅ حفظ `adminCode: "555"` في `UserCubit`
   - ✅ الانتقال لشاشة الأدمن

### 2. إضافة أكواد للطلاب:

1. **بعد تسجيل دخول الأدمن**
2. **اذهب إلى "إدارة الأكواد"**
3. **أضف كود جديد للطالب:**
   - الكود سيتم ربطه تلقائياً بـ `adminCode` المسجل (مثل: "555")
   - الطالب يستخدم هذا الكود لتسجيل الدخول من التطبيق

---

## 🔄 الطريقة القديمة (للمرجع فقط)

<details>
<summary>اضغط للعرض (الطريقة القديمة - لم تعد مطلوبة)</summary>

### 1. Document في Collection `adminCodes` ✅

```
Collection: adminCodes
Document ID: (auto-generated)

Fields:
- adminCode: "mohamed"  ✅
- name: "dfcdwc"        ✅
- description: "ثبثصبصث" ✅
- createdAt: (timestamp) ✅
```

### 2. Document في Collection `codes` ❌ (لم تعد مطلوبة)

```
Collection: codes
Document ID: (auto-generated أو أدخل ID يدوياً)

Fields:
- code: "mohamed"              ⭐ الكود الذي ستستخدمه لتسجيل الدخول
- name: "أدمن"                 ⭐ اسم الأدمن
- phone: "01234567890"         ⭐ رقم الهاتف
- adminCode: "mohamed"         ⭐ نفس قيمة adminCode من adminCodes
- createdAt: (timestamp)       ⭐ تاريخ الإنشاء
```

**ملاحظة:** هذه الطريقة لم تعد مطلوبة. الأدمن يسجل دخول مباشرة من `adminCodes`.

</details>

---

## الخطوات التفصيلية

### الخطوة 1: إنشاء Document في `adminCodes`

1. **افتح Firebase Console**
2. **اذهب إلى Firestore Database**
3. **اختر Collection `adminCodes`**
4. **اضغط على "Add document"**
5. **أضف Fields التالية:**

| Field name    | Type      | Value                                                   |
| ------------- | --------- | ------------------------------------------------------- |
| `adminCode`   | string    | `555` (أو أي كود تريده) ⭐ **الكود المستخدم في Dialog** |
| `name`        | string    | `أدمن` (أو أي اسم)                                      |
| `description` | string    | `وصف اختياري` (اختياري)                                 |
| `createdAt`   | timestamp | اضغط "Set to current time"                              |

### الخطوة 2: التحقق من البيانات

تأكد من أن:

- ✅ Document موجود في `adminCodes` مع `adminCode: "555"` (أو أي قيمة)
- ✅ يمكنك استخدام `adminCode` مباشرة لتسجيل الدخول

---

## مثال كامل

### Document 1: في Collection `adminCodes`

```
Collection: adminCodes
Document ID: UQcZOWeVPZiFEYinhabR

Fields:
- adminCode: "mohamed"
- name: "dfcdwc"
- description: "ثبثصبصث"
- createdAt: 22 December 2025 at 19:01:33 UTC+3
```

### Document 2: في Collection `codes` (يجب إضافته)

```
Collection: codes
Document ID: (auto-generated)

Fields:
- code: "mohamed"                    ⭐ الكود المستخدم في Dialog
- name: "أدمن"                        ⭐ اسم الأدمن
- phone: "01234567890"               ⭐ رقم الهاتف
- adminCode: "mohamed"               ⭐ نفس adminCode من adminCodes
- createdAt: (timestamp)             ⭐ تاريخ الإنشاء
```

---

## كيفية الاستخدام

### تسجيل دخول الأدمن:

1. **افتح التطبيق**
2. **اضغط على زر "كود الأدمن"** في شاشة تسجيل الدخول
3. **أدخل الكود:** `555` (من collection `adminCodes` - Field `adminCode`)
4. **سيتم التحقق:**
   - ✅ البحث عن `adminCode: "555"` في `adminCodes` → موجود
   - ✅ حفظ `adminCode: "555"` في `UserCubit`
   - ✅ الانتقال لشاشة الأدمن

### إضافة أكواد للطلاب:

1. **بعد تسجيل دخول الأدمن**
2. **اذهب إلى "إدارة الأكواد"**
3. **أضف كود جديد:**
   - `code: "STU001"` (الكود الذي يستخدمه الطالب)
   - `name: "أحمد محمد"` (اسم الطالب)
   - `phone: "01234567890"` (رقم الهاتف)
   - `adminCode: "555"` (يتم إضافته تلقائياً من `UserCubit`)
4. **الطالب يستخدم الكود `STU001` لتسجيل الدخول من التطبيق**

---

## ملاحظات مهمة

1. **الكود في Dialog = الكود من `codes`**

   - الكود الذي تدخله في Dialog الأدمن يجب أن يكون موجود في collection `codes`
   - ليس في `adminCodes`

2. **adminCode يجب أن يكون متطابق:**

   - `adminCode` في `codes` = `adminCode` في `adminCodes`
   - مثال: كلاهما `"mohamed"`

3. **يمكن استخدام كود مختلف:**
   - يمكن أن يكون `code` في `codes` مختلف عن `adminCode`
   - مثال:
     - `code: "ADMIN001"` (الكود المستخدم في Dialog)
     - `adminCode: "mohamed"` (يربطه بـ adminCodes)

---

## مثال بديل (كود مختلف عن adminCode)

### Document في `codes`:

```
code: "ADMIN001"           ⭐ الكود المستخدم في Dialog
name: "أدمن"
phone: "01234567890"
adminCode: "mohamed"       ⭐ يربطه بـ adminCodes
createdAt: (timestamp)
```

### Document في `adminCodes`:

```
adminCode: "mohamed"       ⭐ نفس القيمة
name: "dfcdwc"
description: "ثبثصبصث"
createdAt: (timestamp)
```

**الاستخدام:**

- في Dialog الأدمن، أدخل: `ADMIN001`
- سيتم ربطه بـ `adminCode: "mohamed"` تلقائياً

---

## الخلاصة

✅ **أنت تحتاج Document واحد فقط:**

1. Document في `adminCodes` مع `adminCode` (مثل: "555")

✅ **الكود الذي تدخله في Dialog:**

- يجب أن يكون موجود في collection `adminCodes`
- Field name: `adminCode`
- مثال: إذا كان `adminCode: "555"`، أدخل `555` في Dialog

✅ **بعد تسجيل دخول الأدمن:**

- يمكنك إضافة أكواد للطلاب من شاشة "إدارة الأكواد"
- الأكواد الجديدة ستكون مرتبطة تلقائياً بـ `adminCode` المسجل
- الطلاب يستخدمون الأكواد لتسجيل الدخول من التطبيق

✅ **مثال:**

- **الأدمن:** يسجل دخول بـ `555` (من `adminCodes`)
- **الأدمن:** يضيف كود طالب `STU001` (في `codes` مع `adminCode: "555"`)
- **الطالب:** يسجل دخول بـ `STU001` (من `codes`)
